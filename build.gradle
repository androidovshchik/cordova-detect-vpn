apply plugin: "java"

sourceSets.main.java.srcDirs = [ "src/android" ]

repositories {
    jcenter()
}

dependencies {
    compileOnly fileTree(dir: "libs", include: ["*.jar"])
    compileOnly "com.google.android:android:4.1.1.4"
    // compileOnly "org.apache.cordova:framework:8.1.0"
}